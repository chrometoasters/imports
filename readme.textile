Requires redis to be installed.

Installation:

@$ gem install bundler rake@
@$ bundle install@

Run tasks via rake:

@$ rake test          # run unit tests@
@$ rake reset         # wipe clean output and redis data@
@$ rake run           # process input@
@$ rake output:xml    # generate xml output@

etc.

Configuration options are in

@config.yml@

